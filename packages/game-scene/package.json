{
  "name": "@signal-atlas/game-scene",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "typecheck": "tsc --project tsconfig.json --noEmit --pretty false",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@signal-atlas/contracts": "workspace:*",
    "phaser": "3.90.0"
  }
}
