{
  "name": "@signal-atlas/simulation",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "typecheck": "tsc --project tsconfig.json --noEmit --pretty false",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@noble/hashes": "2.2.0",
    "@signal-atlas/contracts": "workspace:*",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@signal-atlas/test-fixtures": "workspace:*"
  }
}
